package org.hopenghou.SpringBootApi;

// import static org.junit.Assert.assertEquals;

// import org.junit.jupiter.api.Test;
// import org.springframework.beans.factory.annotation.Autowired;
// import org.springframework.beans.factory.annotation.Value;
// import org.springframework.boot.test.context.SpringBootTest;
// import org.springframework.boot.test.context.SpringBootTest.WebEnvironment;
// import org.springframework.boot.test.web.client.TestRestTemplate;
// import org.springframework.http.HttpEntity;
// import org.springframework.http.HttpHeaders;
// import org.springframework.http.HttpMethod;
// import org.springframework.http.ResponseEntity;

// mvn test
// @SpringBootTest(webEnvironment=WebEnvironment.RANDOM_PORT)
class SpringBootApiApplicationTests {
		// @Value("${api.key}")
    // private String apiKey;

    // @Test
    // void exampleTest(@Autowired TestRestTemplate restTemplate) {
		// 		HttpHeaders headers = new HttpHeaders();
		// 		headers.set("X-API-KEY", apiKey);
		// 		HttpEntity<Void> requestEntity = new HttpEntity<>(headers);

		// 		ResponseEntity<Object> response = restTemplate.exchange(
    // 			"/api/v1/locations", HttpMethod.GET, requestEntity, Object.class);
		// 		// NOTE: failed to import response classes
		// 		assertEquals("Zero is one", response.getBody(), "Hello World");
    // }
}
